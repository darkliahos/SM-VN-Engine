## Description

Allows for generation of a metadata file for use with the engine, this project may expand to do more
  
### How to Run

1. Build the CLI tool:
npm run build

2. Run it directly via Node:
npm run run-cli
Or execute directly using  ts-node  without compiling first:
npm start